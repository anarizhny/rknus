{
  "name": "rknus",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.js",
    "zip": "node scripts/build.js --zip",
    "build:firefox": "node scripts/build.js --firefox",
    "zip:firefox": "node scripts/build.js --firefox --zip"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
